"""Components V2 layout primitives and per-domain view factories.

This package replaces the legacy `formatting.py` embed builders and the
individual `ui/*.py` view classes. Every user-facing response is rendered as a
`discord.ui.LayoutView` containing Components V2 layout components.
"""

from __future__ import annotations
